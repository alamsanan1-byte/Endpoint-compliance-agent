# Generate a secret: python -c "import secrets; print(secrets.token_urlsafe(32))"
API_KEY=
DATABASE_PATH=./data/compliance.db
POLICY_PATH=./policy/baseline.yaml
# Optional HTTPS webhook. Leave empty for all demos/tests.
WEBHOOK_URL=
# Agent submission destination; remote devices require HTTPS.
COLLECTOR_URL=http://127.0.0.1:8000
